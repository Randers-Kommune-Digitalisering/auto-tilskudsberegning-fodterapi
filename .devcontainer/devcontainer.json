{
	"name": "Node-RED 3",
	 "image": "docker.io/nodered/node-red:latest",
	//"image": "docker.io/nodered/node-red:3.0.2",

	// Features to add to the dev container. More info: https://containers.dev/implementors/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [1880],

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "",

	//"postStartCommand": ["node-red"],
	//	"postAttachCommand": {
   	//	 "server": "npm start"
  	//},
	// "dockerComposeFile": "./docker-compose.yml",
   	// "service": "node-red",
   	"workspaceMount": "source=${localWorkspaceFolder},target=/data,type=bind",
   	"workspaceFolder": "/data",
   	"postStartCommand": ["node-red -u '/data/' /data/src/flows.json"],

	
	"containerEnv": { "NODE_RED_ENABLE_PROJECTS": "false"}
	// NODE_PATH=/usr/src/node-red/node_modules:/data/node_modules
		
	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
